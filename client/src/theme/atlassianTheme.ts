/**
 * Atlassian Design System theme setup.
 * The actual token CSS is imported in main.tsx.
 * This module can be extended for custom token overrides if needed.
 */
export const THEME = 'light' as const;
